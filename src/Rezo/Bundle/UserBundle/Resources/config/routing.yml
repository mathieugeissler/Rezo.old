user_profil_id:
    path:     /{id}
    defaults: { _controller: UserBundle:User:index, id: null }
    requirements:
        id:    \d+

user_profil_name:
    path:     /{username}
    defaults: { _controller: UserBundle:User:index, username: null }